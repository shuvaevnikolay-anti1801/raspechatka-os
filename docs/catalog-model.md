# Каталог товаров и услуг

Первая версия каталога разделяет данные сети на два уровня.

## Общий уровень сети

- `Catalog Item` — единая карточка товара, услуги или комплекта.
- `Catalog Group` — группы каталога.
- `Catalog Unit` — единицы измерения.
- `Catalog Price Type` — типы цен, например розничная и закупочная.
- `Catalog Supplier` — поставщики.

Карточка товара содержит цены, штрихкоды, упаковки, аналоги, характеристики,
компоненты комплекта и правила пополнения складов.

## Уровень франчайзинговой сети

- `Organization` — центральная компания или отдельный франчайзи.
- `Business Point` — физическая точка продаж или производственный центр.
- `Catalog Warehouse` — склад, относящийся к точке.
- `Catalog Assortment` — разрешение продавать конкретный товар в конкретной точке.

Товар не копируется для каждой точки. Центральная карточка остаётся одной, а
доступность, локальная цена и складские пороги задаются отдельно. Это позволяет
централизованно обновлять каталог и одновременно поддерживать разный ассортимент
в разных городах и у разных франчайзи.

## Что намеренно не хранится в карточке

Текущий остаток не является редактируемым полем. В полноценном складском модуле
он будет рассчитываться по журналу движений: приходам, продажам, списаниям,
перемещениям и инвентаризациям. Такой подход сохраняет историю и не даёт остатку
расходиться с документами.

## Следующие слои

1. Документы поступления, продажи, списания и перемещения.
2. Неизменяемый журнал движений и остатки по каждому складу.
3. Разграничение доступа пользователя по организации и точкам.
4. Синхронизация с МоимСкладом через сохранённые внешние идентификаторы.
5. Массовый импорт каталога и цен.
